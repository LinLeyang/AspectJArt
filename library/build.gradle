println "library model start"
apply plugin: 'com.android.library'
apply plugin: 'pentaAsj'



android {
    compileSdkVersion 26

    defaultConfig {
        minSdkVersion 14
        targetSdkVersion 26
        versionCode 1
        versionName "1.0"

        testInstrumentationRunner "android.support.test.runner.AndroidJUnitRunner"

    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
    implementation fileTree(include: ['*.jar'], dir: 'libs')
    implementation 'com.android.support:appcompat-v7:26.1.0'
    compile project(path: ':annotation')
}

buildscript {
    repositories {
        google()
        jcenter()
    }
    dependencies {
        classpath 'org.aspectj:aspectjtools:1.9.1'
    }
}



println "fileupload model start"
apply plugin: 'pentaFileupload'
// then apply the plugin
fileupload {
    url = 'http://10.252.177.108:8080/log/mapping'
    logFile = "log.txt"
    mappingFile = "app/build/outputs/mapping/release/mapping.txt"
}
println "fileupload model end"
// then specify properties for file upload

println "library model end"